---
description: Mostra a próxima tarefa do Kanban EcoMentor (Notion) a implementar, na ordem correta de dependência.
argument-hint: ""
---

Consulte a database "Kanban — Entrega 1" no Notion (data source `collection://7bcb3ed4-78b9-4d18-a25f-c632aad82ba7`, dentro da página https://app.notion.com/p/3cc93299daff8196b82eddaf4b420ed0) e liste as tarefas com `Status` diferente de "Concluído", ordenadas por "Ordem" crescente. Se a ferramenta MCP do Notion não estiver disponível ou o ID acima não resolver, procure a página pelo título "EcoMentor — Kanban AEP 6S" e localize a database a partir dela.

Pegue a primeira tarefa não concluída da lista (a de menor "Ordem") e responda:

1. Nome da tarefa e responsável (Pessoa 1/2/3/Todos).
2. O commit de referência esperado ao final.
3. Qual critério da rubrica ela evidencia.
4. Quais arquivos em `backend/app/` (hoje vazios) essa tarefa deve preencher, com base no mapeamento de camadas do `CLAUDE.md`.
5. Um resumo curto do que precisa ser implementado, sem escrever o código ainda — só o plano.

Se houver uma tarefa "Em Andamento" ou "Revisão", avise disso primeiro (pode ser que o time já esteja no meio de algo e não deva pular para a próxima).
